for (let i = 0; i < 10; i++) {
    console.log(i);
}

// airbnb의 컨벤션에서 권장 
// IE 전체 버전에서 사용 가능
for (const key in ['one', 'two']) {
    console.log(i);
}

// airbnb의 컨벤션에서 권장
// IE전체 버전에서 사용불가 (한국사회에서 사용시 유의)
for (const iterator of object) {
    console.log(i);
}
